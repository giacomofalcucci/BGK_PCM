

* to parallelize using do_concurrent :


CPU: make NV=1 FIX="-stdpar=multicore "
(only for CPU) export ACC_NUM_CORES=16
GPU: make NV=1 FIX="-stdpar=gpu "

-DSINGLEPRECISION for single precision simulation
