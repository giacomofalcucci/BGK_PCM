OPENMP (for CPU) version

To parallelize using OpenMP :
* nvidia compiler: make NV=1 FIX="-mp "
* gfortran compiler: make FIX="-fopenmp "
* intel compiler: make INTEL=1 FIX="-fopenmp "

For single precision add " -DSINGLEPRECISION "
